django>=1.4,<1.5 # Timezone support means 1.4 minimum. Install latest security fixes of Django 1.4, but don't jump to 1.5.
Pillow>=2.0.0 # Might work with earlier versions, but not tested. YMMV. Note that 2.0.0 needed for Mac users.
South>=0.7.6 # Might work with earlier versions, but not tested.
Sphinx